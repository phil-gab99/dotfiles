#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
    char *str = "r492(echo";
    char *str2 = "h)";
    char *parenPos = strstr(str, "(");
    /* printf("%s\n", str); */
    /* printf("%s %s\n", str, parenPos); */
    /* printf("%ld\n", strlen(str) - strlen(parenPos)); */
    /* printf("%ld\n", strlen(parenPos + 1)); */
    /* printf("%ld\n", strlen(str2) - 1); */

    char *test = (char *) malloc(sizeof(char) * 3);
    test[2] = '\0';
    memcpy(test, str, 2);
    printf("%s\n", test);
    
    return 0;
}
