#!/bin/bash

echo -n "Specify the backing store file> "
read BACKING_STORE

echo -n "Specify the commands input file> "
read INPUT

echo "Running ./src/TP3 $BACKING_STORE $INPUT"

# valgrind --leak-check=full --leak-resolution=med --trace-children=no --track-origins=yes --vgdb=no --log-file=valgrind-out.txt ./src/TP3 $BACKING_STORE $INPUT
